# [Optional] Customizations for the devcontainer
# This Dockerfile is only needed if you want to add extra tools or dependencies
# The default image and features in devcontainer.json should be sufficient for most scenarios
FROM mcr.microsoft.com/devcontainers/dotnet:9.0

# Example: install additional tools
# RUN apt-get update && apt-get install -y <your-package>
