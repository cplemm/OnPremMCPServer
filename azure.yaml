# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: on-prem-mcpserver
metadata:
    template: azd-init@1.14.0
services:
    mcpserver:
        project: app/mcpserver
        host: containerapp
        language: dotnet
        docker:
            path: Dockerfile
    webclient:
        project: app/webclient
        host: containerapp
        language: dotnet
        docker:
            path: Dockerfile
